# Files that live in the repo but should not be applied to $HOME.
# chezmoi reads this with shell-style globs (gitignore-like syntax).

# Installer + repo meta
install.sh
README.md
HANDOFF.md
Dockerfile
.gitignore

# wallpapers/ is a source for install.sh, not a managed dotfile.
wallpapers
wallpapers/**

# Never let chezmoi manage shell history — it is per-machine state.
.zsh_history
